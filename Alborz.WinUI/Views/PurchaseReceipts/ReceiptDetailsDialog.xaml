<ContentDialog
    x:Class="Alborz.WinUI.Views.PurchaseReceipts.ReceiptDetailsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:Alborz.WinUI.Converters"
    Title="Purchase Receipt Details"
    CloseButtonText="Close"
    DefaultButton="Close"
    CornerRadius="8">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1200</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">800</x:Double>

        <converters:CurrencyFormatConverter x:Key="CurrencyConverter" />
    </ContentDialog.Resources>

    <Grid Width="900" Height="650" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Supplier:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Bind Receipt.SupplierName}" FontWeight="SemiBold" FontSize="16"/>

                <TextBlock Grid.Row="0" Grid.Column="1" Text="Date:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind Receipt.ReceiptDate}" FontWeight="SemiBold" FontSize="16"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Ref Number:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="{x:Bind Receipt.ReferenceNumber}" FontWeight="SemiBold" FontSize="16"/>
            </Grid>
        </Border>

        <controls:DataGrid Grid.Row="1" 
                           ItemsSource="{x:Bind Receipt.Items, Mode=OneWay}" 
                           AutoGenerateColumns="False" 
                           IsReadOnly="True" 
                           GridLinesVisibility="Horizontal"
                           VerticalAlignment="Stretch">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*" MinWidth="250"/>
                <controls:DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                <controls:DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, Converter={StaticResource CurrencyConverter}}" Width="140"/>
                <controls:DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount, Converter={StaticResource CurrencyConverter}}" Width="120"/>
                <controls:DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice, Converter={StaticResource CurrencyConverter}}" Width="160"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <Border Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="300" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <TextBlock Text="Remarks:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{x:Bind Receipt.Remarks}" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorPrimaryBrush}" MaxHeight="80"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="8" VerticalAlignment="Bottom">
                    <Grid>
                        <TextBlock Text="Total Amount:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Receipt.TotalAmount, Converter={StaticResource CurrencyConverter}}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid>
                        <TextBlock Text="Global Discount:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Receipt.TotalDiscount, Converter={StaticResource CurrencyConverter}}" Foreground="IndianRed" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid>
                        <TextBlock Text="Additional Charges:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Receipt.AdditionalCharges, Converter={StaticResource CurrencyConverter}}" Foreground="SeaGreen" HorizontalAlignment="Right"/>
                    </Grid>

                    <MenuFlyoutSeparator Margin="0,4" />

                    <Grid>
                        <TextBlock Text="Net Amount:" Style="{ThemeResource SubtitleTextBlockStyle}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Bind Receipt.NetAmount, Converter={StaticResource CurrencyConverter}}" Style="{ThemeResource TitleTextBlockStyle}" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</ContentDialog>