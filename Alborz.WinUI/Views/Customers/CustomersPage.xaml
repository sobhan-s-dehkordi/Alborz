<Page
    x:Class="Alborz.WinUI.Views.Customers.CustomersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:Alborz.WinUI.Converters"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:CurrencyFormatConverter x:Key="CurrencyConverter" />
    </Page.Resources>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filter Bar -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- 1. Filter Bar -->
        <Border Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Header="Name" Text="{x:Bind ViewModel.SearchName, Mode=TwoWay}" PlaceholderText="Search by name..."/>
                <TextBox Grid.Column="1" Header="Phone" Text="{x:Bind ViewModel.SearchPhone, Mode=TwoWay}" PlaceholderText="e.g. 0912..."/>
                <TextBox Grid.Column="2" Header="National Code" Text="{x:Bind ViewModel.SearchNationalCode, Mode=TwoWay}" PlaceholderText="10 digits..."/>

                <Button Grid.Column="3" Command="{x:Bind ViewModel.LoadCustomersCommand}" Style="{ThemeResource AccentButtonStyle}" VerticalAlignment="Bottom" Width="100">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" FontSize="14"/>
                        <TextBlock Text="Search" Margin="0,0,0,2"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- 2. DataGrid -->
        <Border Grid.Row="1" BorderBrush="{ThemeResource SystemControlForegroundChromeDisabledLowBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <controls:DataGrid ItemsSource="{x:Bind ViewModel.Customers, Mode=OneWay}" 
                               SelectedItem="{x:Bind ViewModel.SelectedCustomer, Mode=TwoWay}"
                               AutoGenerateColumns="False" 
                               IsReadOnly="True" 
                               GridLinesVisibility="Horizontal"
                               SelectionMode="Single">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <controls:DataGridTextColumn Header="Customer Name" Binding="{Binding Name}" Width="*" MinWidth="200"/>
                    <controls:DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="150"/>
                    <controls:DataGridTextColumn Header="National Code" Binding="{Binding NationalCode}" Width="150"/>
                    <controls:DataGridTextColumn Header="Balance" Binding="{Binding Balance, Converter={StaticResource CurrencyConverter}}" Width="150"/>
                    <controls:DataGridTextColumn Header="Loyalty Points" Binding="{Binding LoyaltyPoints}" Width="120"/>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>

        <!-- 3. Footer (Action Buttons) -->
        <Border Grid.Row="2" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">

                    <Button Content="Refresh" Command="{x:Bind ViewModel.LoadCustomersCommand}" Width="100"/>

                    <Button Content="Edit Customer" 
                            Command="{x:Bind ViewModel.ShowEditDialogCommand}" 
                            IsEnabled="{x:Bind ViewModel.IsCustomerSelected, Mode=OneWay}"
                            Width="130"/>

                    <Button Content="New Customer" 
                            Command="{x:Bind ViewModel.ShowAddDialogCommand}" 
                            Style="{ThemeResource AccentButtonStyle}"
                            Width="140">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE710;" FontSize="14"/>
                                    <TextBlock Text="New Customer"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>