<Page
 FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}"    x:Class="Alborz.WinUI.Views.Reports.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" x:Name="Heading" FontSize="24" FontWeight="SemiBold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <CalendarDatePicker FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" Header="From" Date="{x:Bind ViewModel.FromDate, Mode=TwoWay}"/>
            <CalendarDatePicker FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" Header="To" Date="{x:Bind ViewModel.ToDate, Mode=TwoWay}"/>
            <TextBox FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" x:Name="ProductFilter" Header="Product ID" Width="130" Text="{x:Bind ViewModel.ProductId, Mode=TwoWay}"/>
            <Button FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" Content="Load report" VerticalAlignment="Bottom" Command="{x:Bind ViewModel.LoadCommand}"/>
            <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Width="24" Height="24"/>
        </StackPanel>
        <TextBlock FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" Grid.Row="2" Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" TextWrapping="Wrap"/>
        <controls:DataGrid FontSize="{Binding FontSize, Source={StaticResource Appearance}}" FontFamily="{Binding FontFamily, Source={StaticResource Appearance}}" Grid.Row="3" ItemsSource="{x:Bind ViewModel.Rows}" AutoGenerateColumns="False" IsReadOnly="True">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <controls:DataGridTextColumn Header="Date" Binding="{Binding Date}"/>
                <controls:DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <controls:DataGridTextColumn Header="Quantity / Count" Binding="{Binding Quantity}"/>
                <controls:DataGridTextColumn Header="Amount" Binding="{Binding Amount}"/>
                <controls:DataGridTextColumn Header="Stock balance" Binding="{Binding Balance}"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>

