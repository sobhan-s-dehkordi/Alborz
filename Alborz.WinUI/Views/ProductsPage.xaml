<Page
    x:Class="Alborz.WinUI.Views.ProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- بخش ثبت کالا -->
        <Border Grid.Column="0" 
                BorderBrush="{ThemeResource SystemControlForegroundChromeDisabledLowBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="16" 
                Margin="0,0,16,0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <StackPanel Spacing="12">
                <TextBlock Text="Register New Product" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,8"/>
                <TextBox Header="Product Name" Text="{x:Bind ViewModel.ProductName, Mode=TwoWay}" />
                <TextBox Header="Barcode" Text="{x:Bind ViewModel.Barcode, Mode=TwoWay}" />
                <TextBox Header="Purchase Price" Text="{x:Bind ViewModel.PurchasePrice, Mode=TwoWay}" />
                <TextBox Header="Sell Price" Text="{x:Bind ViewModel.SellPrice, Mode=TwoWay}" />
                <TextBox Header="Initial Stock" Text="{x:Bind ViewModel.StockQuantity, Mode=TwoWay}" />
                <TextBox Header="Reorder Point" Text="{x:Bind ViewModel.ReorderPoint, Mode=TwoWay}" />

                <Button Content="Save Product" Command="{x:Bind ViewModel.AddProductCommand}" 
                        HorizontalAlignment="Stretch" Margin="0,16,0,0"/>

                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- بخش لیست کالاها -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,16">
                <TextBox PlaceholderText="Search..." Text="{x:Bind ViewModel.SearchTerm, Mode=TwoWay}" Width="300" VerticalAlignment="Center"/>
                <Button Content="Search" Command="{x:Bind ViewModel.LoadProductsCommand}" VerticalAlignment="Center"/>
            </StackPanel>

            <controls:DataGrid Grid.Row="1" ItemsSource="{x:Bind ViewModel.Products, Mode=OneWay}" 
                               AutoGenerateColumns="False" IsReadOnly="True">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <controls:DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="150"/>
                    <controls:DataGridTextColumn Header="Sell Price" Binding="{Binding SellPrice}" Width="120"/>
                    <controls:DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="100"/>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
    </Grid>
</Page>